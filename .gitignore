gong.env
gong.env.bak
settings.json
projects.json
# Anchored to the repo root — a bare `transcripts/` also matched
# app/api/gong/transcripts/, silently excluding that route from every commit
# until this was caught. Same class of bug .next-*/ below was already fixed
# for once.
/raw/
/transcripts/
/sorted/
/documents/
/logs/
.server.pid
node_modules/
ui/_probe.html

# Next.js
.next/
# Isolated builds used for testing without disturbing a running dev/prod
# server (NEXT_DIST_DIR=.next-<name> npx next build) — never real output.
.next-*/
next-env.d.ts

# SQLite — the whole application state lives here
data.db
data.db-shm
data.db-wal

# Superseded JSON stores, kept by the migration rather than deleted
projects.json.migrated
settings.json.migrated
warp-context/
